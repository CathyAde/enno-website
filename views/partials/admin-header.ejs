<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Admin ENNO' %></title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    :root {
      --primary: #0ea5e9;
      --secondary: #6366f1;
      --dark: #0b1224;
      --light: #e2e8f0;
    }
    
    body {
      background: #f8fafc;
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
    }
    
    .sidebar {
      background: var(--dark);
      color: var(--light);
      min-height: 100vh;
    }
    
    .nav-link {
      display: flex;
      align-items: center;
      padding: 0.75rem 1rem;
      color: #94a3b8;
      text-decoration: none;
      border-radius: 0.5rem;
      transition: all 0.2s;
    }
    
    .nav-link:hover {
      background: rgba(255, 255, 255, 0.05);
      color: var(--light);
    }
    
    .nav-link.active {
      background: linear-gradient(135deg, var(--primary), var(--secondary));
      color: white;
    }
    
    .badge {
      background: linear-gradient(135deg, var(--primary), var(--secondary));
      color: white;
      padding: 0.25rem 0.5rem;
      border-radius: 9999px;
      font-size: 0.75rem;
      font-weight: 600;
    }
  </style>
</head>
<body>
  <div class="flex">
    <!-- Sidebar -->
    <div class="sidebar w-64 p-6">
      <div class="mb-8">
        <div class="flex items-center gap-3 mb-2">
          <div class="w-10 h-10 rounded-lg bg-gradient-to-br from-blue-500 to-cyan-400 flex items-center justify-center">
            <span class="text-white font-bold">EN</span>
          </div>
          <div>
            <div class="font-bold text-white">ENNO Admin</div>
            <div class="text-xs text-blue-300">Tableau de bord</div>
          </div>
        </div>
        <div class="text-sm text-blue-200 mt-4">
          Connecté en tant que <span class="font-semibold"><%= user.email %></span>
        </div>
      </div>
      
      <nav class="space-y-2">
        <a href="/admin" class="nav-link <%= title === 'Dashboard' ? 'active' : '' %>">
          <span>📊</span>
          <span class="ml-3">Tableau de bord</span>
        </a>
        
        <a href="/admin/services" class="nav-link <%= title === 'Gestion des services' ? 'active' : '' %>">
          <span>⚙️</span>
          <span class="ml-3">Services</span>
        </a>
        
        <a href="/admin/messages" class="nav-link <%= title === 'Messages' ? 'active' : '' %>">
          <span>📨</span>
          <span class="ml-3">Messages</span>
          <span class="badge ml-auto">12</span>
        </a>
        
        <a href="/admin/content" class="nav-link <%= title === 'Contenu' ? 'active' : '' %>">
          <span>📝</span>
          <span class="ml-3">Contenu</span>
        </a>
        
        <div class="pt-6 mt-6 border-t border-gray-700">
          <a href="/admin/logout" class="nav-link text-red-300 hover:text-red-100 hover:bg-red-900/20">
            <span>🚪</span>
            <span class="ml-3">Déconnexion</span>
          </a>
        </div>
      </nav>
    </div>
    
    <!-- Main Content -->
    <div class="flex-1 p-8">